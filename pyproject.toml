[project]
name = "bookalign"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ebooklib>=0.20",
    "lxml>=5.0",
    "pysbd>=0.3.4",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]
