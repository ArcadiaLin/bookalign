# Examples

这里预留两个公开展示时最直观的样例位置：

- `examples/金阁寺-段落.epub`
- `examples/哈利波特全集-段落.epub`

这两个文件建议只在本地保留，不直接提交到公开仓库。

原因很简单：

- 生成结果仍然来自受版权保护的原著和译本
- 把完整 EPUB 样例直接放到 GitHub 仓库里不合适

如果你本地已经生成过它们，可以把文件复制到这里用于截图、演示或阅读器兼容性检查。

README 里的配图建议单独放在 `docs/images/` 下。
